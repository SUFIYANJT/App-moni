<?xml version="1.0" encoding="utf-8"?>

<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/darkBackground</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="progressBar">
        <item name="indicatorColor">@color/progress_indicator_color</item>
        <item name="trackColor">@color/progress_track_color</item>
    </style>
    <style name="ActivityMain">
        <item name="background">@color/id</item>
        <item name="iconTint">@color/darkBackground</item>
        <item name="android:textColor">@color/darkBackground</item>
        <item name="android:backgroundTint">@color/id</item>
    </style>
    <style name="Cantainer" >
        <item name="android:background">@color/id</item>
        <item name="android:textColor">@color/lightDark</item>
    </style>
    <style name="CardViewStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@color/ss</item>
    </style>
    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />
</resources>